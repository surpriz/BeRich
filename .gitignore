# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# BeRich data cache & ML artifacts
data/
mlruns/
mlartifacts/
mlflow.db
*.parquet.tmp

# Deployment secrets — these live outside the repo (see docs/DEPLOY.md), but
# block them here as a tripwire in case someone ever copies /etc/berich/ in.
/etc/berich/
